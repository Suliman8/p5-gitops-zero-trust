# demo namespace, NOW labeled for Istio sidecar injection.
# Pods created here get an istio-proxy sidecar injected automatically
# by the Istio mutating webhook.
apiVersion: v1
kind: Namespace
metadata:
  name: demo
  labels:
    kubernetes.io/metadata.name: demo
    istio-injection: enabled
