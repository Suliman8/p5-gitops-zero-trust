# ServiceAccount = SPIRE Server's IDENTITY inside Kubernetes.
# Without this, SPIRE would run as the `default` SA with no permissions and
# couldn't call the K8s TokenReview API to validate node tokens.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: spire-server
  namespace: spire-server
